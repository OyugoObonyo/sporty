ISSUES: -SATURDAY - DEPLOY
        SATURDAY- Check how to generate a hashed hardcoded string and generate string== os.urandom.hex() 
            
        FUTURE UPDATE == Integrate Online paymenst to complete orders
        TOPIC FOR FURTHER DISCUSSION: advantages and disads of using id/uuid on routes
        FUTURE UPDATE - Remove deleted products from session <extend a delete_item function>
        (-Potential solutions:
          1) Add stock count to product model and limit add to cart functionality in cases where
              stock count == 0. Only delete products whose stock count == 0  from session
              )          )
        Timeout - 4PM!